:PROPERTIES:
:ID:       83feb6b0-a900-46a4-9e3d-2ccb8d00e8d6
:ROAM_REFS: https://cachestocaches.com/2016/9/my-workflow-org-agenda/
:END:
#+date: [2020-06-16 Tue]
#+title: Org Mode
#+OPTIONS: toc:nil ^:nil
#+filetags: :computers:

- [[id:D40E4A55-B82B-401F-926C-41FA49492C0E][Citations in Org Mode]]
- [[id:8775C752-CD57-4F55-9BB5-436CA6CA07FC][Archive completed tasks in Org mode]]
- [[id:9F2EE6DF-86A2-49FB-B5A2-E6D03B01112D][Latex & Python Tricks in Org]]
- [[id:5d7a5064-6bd7-497b-99a7-2ce8c6576459][Emacs]]

* Show subtree in separate buffer

Use =org-tree-to-indirect-buffer= command (=C-c C-x b=)

* Remove paragraph indent while exporting to latex

#+begin_src org
#+begin_export latex
\setlength{\parindent}{0pt}
#+end_export
#+end_src


* Set org babel header arg for all blocks in file

Write the following at start of the file to add header arguments
=:session *Python* :results output :tangle solution.py= to all python
source blocks in the file.

#+begin_src org
#+PROPERTY: header-args:python :session *Python* :results output :tangle solution.py
#+end_src

This works because org source blocks header can be specified in
property drawers (See info manual [[info:org#Using Header Arguments][org#Using Header Arguments]]). And
=#+PROPERTY:= is just a way to specify properties for a file.

=header-args= property applies to all source blocks while
=header-args:prop= applies to =prog= language source blocks.

* Use svg image for html and pdf for LaTeX

#+begin_src org
#+MACRO: img (eval (format "[[file:%s.%s]]" $1 (if (org-export-derived-backend-p org-export-current-backend 'latex) "pdf" "svg")))
#+end_src

Add this line in the org file to create a macro. Then use it as follows (don't add the extension at the end):
#+begin_src org
{{{img(path/to/image)}}}
#+end_src

* Source block without babel backend

When using source blocks that don't have a babel backend add the parameter =:exports code=
to prevent org-mode from complaining about the backend being not found. Because even though the =:eval no-export= is present, org babel still tries to parse the params and do some stuff with the source block.

So instead of
#+begin_src org
  ,#+begin_src json
    {
      "name" : "Bibek Panthi",
      "species" : "Homo sapiens"
    }
  ,#+end_src
#+end_src

do
#+begin_src org
  ,#+begin_src json :exports code
    {
      "name" : "Bibek Panthi",
      "species" : "Homo sapiens"
    }
  ,#+end_src
#+end_src

* Literate DevOps

https://howardism.org/Technical/Emacs/literate-devops.html

You can run org babel  =sh= source blocks on remote systems using =:dir= parameter.

Consider the following:

#+begin_src org
  ,#+begin_src sh :dir /etc :results raw :wrap verbatim
  head passwd
  ,#+end_src
#+end_src

This is same as:
#+begin_src org
  ,#+begin_src sh :results raw :wrap verbatim
  head /etc/passwd
  ,#+end_src
#+end_src

The =:dir= parameter accepts anything that emacs accepts for opening files, including paths opened by tramp. For example, if you have a server =server.com= and you open some file by =M-x find-file= =/ssh:server.com:/home/bpanthi977/test.org=. Then in similar way, you can specify =:dir= parameter to work on a remote directory:

#+begin_src org
  ,#+begin_src sh :dir /ssh:server.com:/etc :wrap verbatim :results raw
  head passwd
  ,#+end_src

  ,#+RESULTS:
  ,#+begin_verbatim
  root:x:0:0:root:/root:/bin/bash
  bin:x:1:1:bin:/bin:/sbin/nologin
  daemon:x:2:2:daemon:/sbin:/sbin/nologin
  adm:x:3:4:adm:/var/adm:/sbin/nologin
  lp:x:4:7:lp:/var/spool/lpd:/sbin/nologin
  sync:x:5:0:sync:/sbin:/bin/sync
  shutdown:x:6:0:shutdown:/sbin:/sbin/shutdown
  halt:x:7:0:halt:/sbin:/sbin/halt
  mail:x:8:12:mail:/var/spool/mail:/sbin/nologin
  operator:x:11:0:operator:/root:/sbin/nologin
  ,#+end_verbatim
#+end_src


* Show headings in beamer

Add the following to the top of org file:
#+begin_verse
#+begin_export latex
\\AtBeginSection[]{
  \\begin{frame}
  \\vfill
  \\centering
  \\begin{beamercolorbox}[sep=8pt,center,shadow=true,rounded=true]{title}
    \\usebeamerfont{title}\\insertsectionhead\\par%
  \\end{beamercolorbox}
  \\vfill
  \\end{frame}
}
#+end_export
#+end_verse

* Add section break after each section

#+begin_verse
#+begin_export latex
\let\oldsection\section
\renewcommand\section{\clearpage\oldsection}

\clearpage
#+end_export
#+end_verse

* No section numbering in latex export

#+begin_verse
#+BEGIN_EXPORT latex
\let\oldsection\section
\renewcommand\section{\clearpage\oldsection*}

\clearpage
#+END_EXPORT
#+end_verse

* Colorful Listings

#+begin_verse
#+begin_export latex
\usepackage{listings}
\usepackage{color}
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}


\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=5pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=2,
    upquote=true
}

\lstset{style=mystyle}
#+end_export
#+end_verse

* Agenda

[[https://cachestocaches.com/2016/9/my-workflow-org-agenda/]]

* Show image in org buffer but not in exports

For this you can wrap the image link/attachment inside a drawer. And
then configure emacs to not export that drawer.

For example:

#+begin_src org
  ,#+OPTIONS: d:(not "noexport" "logbook")

  :noexport:
  [[attachment:image.png]]
  :end:
#+end_src

It can be configured globally by setting the value of
=org-export-with-drawers=. It is a list of drawers to be exported or
if the list starts with =not=, it the list of drawers to not export.

#+begin_src elisp
  (setq org-export-with-drawers '(not "noexport" "logbook"))
#+end_src

The default value of =org-export-with-drawers= is =(not "LOGBOOK")=
that's why in above examples we keep that value too and also specify
=noexport= drawers to be not exported.

Alternatively, you can specify =d:nil= to prevent explorting any
drawers.

* Tips for writing latex

Use org-cdlatex-mode

From: https://karthinks.com/software/latex-input-for-impatient-scholars/#snippets-snap-easy
#+begin_quote
The goal is to write LaTeX (and especially math) in a way that mirrors
our stream of thought, where our eyes, fingers and the cursor all glide
over the clutter of the markup. The above described process isn't quite
there yet, but it's close. In summary,

- Turn on =prettify-symbols-mode= to enhance readability.
- Enter symbols with grave (=`=) and modify text or macros with the
  quote (='=).
- Use =TAB= to jump forward to the next “point of interest” when typing.
- Insert environment and float templates with CDLaTeX's commands or YaS
  snippets. =env <tab>= for an arbitrary environment (with completion).
- Use YaS' auto-expanding snippets (or =abbrev-mode= abbrevs) to type
  without breaking the flow. Generally useful, but especially handy for
  math operators.
- Preview your work with =preview-= commands. =preview-at-point= is
  quite handy.
- When everything else fails, use the slower but fully document aware
  AucTeX commands: =LaTeX-environment= (=C-c C-e=) and
  =TeX-insert-macro= (=C-c C-m=).
#+end_quote


* From Tutorial :noexport:
** Tasks
** DONE Start new org file for tutorial
   CLOSED: [2020-04-24 Fri 13:37]
** DONE Outline Document
   CLOSED: [2020-04-24 Fri 13:51]
** DONE Write Introduction
   CLOSED: [2020-04-24 Fri 13:54]
** TODO Finish Document
   SCHEDULED: <2020-04-25 Sat>

** Introduction
Try M-S-Ret to add a todo header
or M-Ret to add a normal header
** Obtaining org-mode
+ Links :: Try moving the cursor over the link and press C-c C-o for org-open-at-point
[[http://orgmode.org][Org Website]]

** Installation
** Configurations
** The Basics
** Creating a new file
** Simple task list
** Using headlines to structure a documnt
** Links to other place
** Adding notes to a task
** Scheduling an appointment
** Using the agenda

** A taste of advanced features
Before we finish, we'll touch on org-mode's global TODO list, scheduling, and agenda features. These are used to combine relevant TODO items from different org files into a prioritized, chronological display. You can also use it to see which tasks were completed, and when.

** Setup
*org-agenda-files* must be set to files to search for TODOs and scheduled items.

*C-c a t* to enter the global todo list

** Links
** Insert
C-c C-l
** Edit
C-c C-l
** Goto / Visit
C-c C-o
